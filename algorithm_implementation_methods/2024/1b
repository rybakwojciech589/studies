#include <bits/stdc++.h>
using namespace std;

//#define int long long
#define pb push_back
#define st first
#define nd second
#define ll long long
const int inf=1e9+7;
const int infl=1e18+93;
vector<char> wek;

signed main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    string a;
    cin>>a;
    
    for(int i=0;i<a.size();i++)
    {
        int b=int(a[i]-'0');
        if((b)%8==0)
        {
            cout<<"YES\n";
            cout<<b<<"\n";
            return 0;
        }
    }
    if(a.size()>1) 
    {
        for(int i=0;i<a.size();i++)
        {
            for(int j=i+1;j<a.size();j++)
            {
                    int b=int(a[i]-'0'), c=int(a[j]-'0');
                    if(b==0) continue;
                    if((b*10+c)%8==0)
                    {
                        cout<<"YES\n";
                        cout<<b*10+c<<"\n";
                        return 0;
                    }
            }
        }
    }
    if(a.size()>2)
    {
        for(int i=0;i<a.size();i++)
        {
            for(int j=i+1;j<a.size();j++)
            {
                for(int k=j+1;k<a.size();k++)
                {
                    int b=int(a[i]-'0'), c=int(a[j]-'0'), d=int(a[k]-'0');
                    if(b==0) continue;
                    if((b*100+c*10+d)%8==0)
                    {
                        cout<<"YES\n";
                        cout<<b*100+c*10+d<<"\n";
                        return 0;
                    }
                }
            }
        }        
    }
    cout<<"NO\n";
}
